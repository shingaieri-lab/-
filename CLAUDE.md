# Rules

- Always commit directly to the main branch
- Never create pull requests
- Never create new branches
- Never auto-commit without explicit user instruction
- Never auto-push without explicit user instruction
