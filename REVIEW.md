# IS進捗管理ツール UXレビュー

レビュー日：2026-03-23　／　最終更新：2026-03-31

---

## このツールは何をするもの？

営業チームが「どの会社に連絡したか」「次はいつ連絡するか」を管理するツール。
AIが通話メモを読んで次の行動を提案したり、Googleカレンダーの空き時間を自動で探したりできる。

---

## ✅ 対応済み一覧（全体）

### UX・デザイン

| 問題 | 対応内容 | コミット |
|------|---------|---------|
| 候補日→メールのリード引き継ぎがなかった | リード・テンプレートを自動引き継ぎ | `3845520` |
| AIページだけ見た目が全然違った（濃紺） | ライトグリーン系に統一 | `aed959f` |
| ナビラベルが小さすぎた（8〜9.5px） | 10pxに拡大 | `ac443b0` |
| コントラスト不足（読み取り専用フィールド） | テキスト色を `#3d7a5e` に変更（WCAG AA準拠） | `61fd7e4` |
| メールページの変数一覧が見えにくかった | `fontSize:12, color:#6b7280` に変更 | `61fd7e4` |
| テンプレートの選択状態が不明瞭だった | 背景 `#d1fae5`・ボーダー `#10b981` に変更 | `61fd7e4` |
| 設定の初回セットアップが難しすぎた | Gemini・Gmail・Googleカレンダーの設定ウィザードを実装 | `bf7545e` |

### コード品質

| 問題 | 対応内容 | コミット |
|------|---------|---------|
| Gmail下書き保存のコードが2か所に重複 | 共通関数に統合 | `998803f` |
| 祝日リストが2か所に重複定義 | グローバル変数に一本化 | `998803f` |
| メールテンプレート初期値が2か所でズレ | 1か所に統一 | `998803f` |

### セキュリティ

| 問題 | 対応内容 | コミット |
|------|---------|---------|
| パスワードが暗号化なしで保存されていた | `bcryptjs` ハッシュ化・招待コード制を実装 | `ac1bdfb` |

### バグ修正（2026-03-25対応分）

| 優先度 | 問題 | 対応内容 | コミット |
|--------|------|---------|---------|
| 🔴 高 | パスワード再設定の競合状態 | `r.ok` 先チェック、非JSON応答に `.catch(()=>({}))` 対応 | `08cc4eb` |
| 🔴 高 | Gmail OAuthトークンに有効期限なし | `isTokenValid()` で55分キャッシュ実装 | `08cc4eb` |
| 🔴 高 | APIキー参照が混在（`effectiveAiConfig`） | 優先順位をコメントで明示 | `08cc4eb` |
| 🟠 中 | fetchエラーのサイレント無視（`AccountManager`） | `.catch(err => console.error(...))` に変更 | `08cc4eb` |
| 🟠 中 | AI保存ボタンが3秒後に消える | 「✅ 保存済（再保存）」ボタンに変更 | `08cc4eb` |
| 🟠 中 | 日時フォーマットの混在 | `normalizeDate` 経由で `YYYY-MM-DD` に統一 | `08cc4eb` |
| 🟡 低 | OAuthスクリプト重複ロード | `document.querySelector` チェックを追加 | `08cc4eb` |
| 🟡 低 | メール署名の部分一致誤判定 | `senderSig.trim()` で正規化 | `08cc4eb` |
| 🟡 低 | 環境判定が曖昧（`server.js`） | `RUN_SERVER=1` で強制起動対応 | `08cc4eb` |
| 🟡 低 | SJIS判定ロジックの誤り | バイトペア検証に改善 | `08cc4eb` |
| 🟡 低 | ストレージ上限の未対応 | HTTP 413/507 でアラート通知 | `08cc4eb` |

---

## ✅ 新規追加機能（2026-03-25 以降）

### Zoho CRM 連携

| 対応内容 | コミット |
|---------|---------|
| Zoho CRM API連携を実装（商談自動作成） | `ed31ccf` |
| アクション履歴のZoho連携をNoteから行動（Calls）に変更 | `ed96d80` |
| 商談作成の重複防止をサーバー側で実装 | `4a0489f` |
| 商談実施日・完了予定日のAPI名を設定画面から入力できるよう対応 | `783ef89` |
| リード項目に住所フィールドを追加（Zoho State+City+Street から連携） | `c93aeb3` |
| IS担当フラグをアカウントに追加（Webhookのメンバーフィルタ用） | `3c47b17` |
| Zoho手動取込UIをリード管理画面に追加 | `6f8ac5c` |

### KPI・ダッシュボード

| 対応内容 | コミット |
|---------|---------|
| 有効リード数の追加と商談化率の計算変更 | `58cd9a0` |
| 月別詳細データに各月の合計行を追加 | `483e8e6` |
| 月別詳細データの率表示を小数点第一位まで変更 | `bfef80c` |

### その他

| 対応内容 | コミット |
|---------|---------|
| ツール名を「IS進捗管理」に統一 | `177426e` |

---

## ❌ 未対応：今後対応が必要なもの

### 🔴 高優先度

#### モバイル対応の不完全さ（メール・AI・設定ページ）

`isMobile` フラグでレイアウトを切り替えているが、一部の画面でカバーが不完全。

- AIページ左パネル：`width:380`（固定）→ モバイルで全幅を圧迫
- メールテンプレート：`gridTemplateColumns:"240px 1fr"`（固定）
- 設定ページのタブ：`width:"60vw"`（固定）→ モバイルでオーバーフローの可能性

**改善案：** ブレークポイントで固定幅をすべて可変に。ボタンパディングをモバイルで拡大。

---

### 🟠 中優先度

#### 削除・編集ボタンが初期状態で非常に薄い（設定ページ）

`opacity:0.3` で表示され、ホバー時だけ `opacity:1` になる。タッチデバイスではホバーが発生しないため、ボタンの存在に気づけない。

**改善案：** 常時 `opacity:0.7` 以上に変更。

#### 成功メッセージの消失が速すぎる（全体）

「✅ 下書き保存済」が3秒、「保存しました ✓」が2秒で自動消失。読む前に消えるケースがある。

**改善案：** 5〜8秒に延長、または ✕ ボタンで手動 Close。

#### エラーメッセージの視認性が低い（AIページ・フォーム全般）

エラー背景が薄く（`#ef444416`）、複数エラー時は一括 `alert()` で表示されるため、どのフィールドに問題があるか分かりにくい。

**改善案：** 背景を濃くしアイコンを付ける。各フィールドに個別エラーを表示。

#### ボタンが小さすぎてタッチ操作しにくい（テンプレート・設定リスト）

`padding:"3px 10px"`, `fontSize:12` で高さ約20px。WCAG 推奨の最小タッチ領域は 44×44px。

**改善案：** 最低 `padding:"6px 12px"`（高さ32px以上）に統一。

#### バルク操作の完了通知がない（リード管理）

複数リードのインポート・削除完了時に視覚的フィードバックがなく、処理結果が分かりにくい。

**改善案：** 画面右下にトースト通知（例：「10件追加しました ✓」）を実装。

#### ローディング表示が統一されていない（全体）

AI解析中はスピナーが表示されるが、Gmail保存・Googleタスク作成などはテキスト変化のみ。

**改善案：** 全ての非同期処理で同じスピナーを使用。

---

### 🟡 低優先度（余裕があれば）

#### AIページ：解析後の導線が不明

解析→保存した後、次に何をすればいいかの案内がない。

```
現状： 解析する → 保存完了！ ← ここで終わり

改善案：解析する → 保存完了！
                 → 「📧 フォローメールを送る」ボタン
                 → 「📅 商談候補日を探す」ボタン
```

#### 確認ダイアログがブラウザのポップアップ

削除確認に `window.confirm`（ブラウザ標準のグレーのポップアップ）を使用。
アプリのデザインと統一感がなく、ユーザーが戸惑う。

**改善案：** アプリ内モーダルで確認を取る。

#### 単一HTMLファイルの分割（長期対応）

全ロジック・スタイル・テンプレートが1ファイルに集中しており、機能追加・バグ修正の難易度が高い。
長期的にはコンポーネントごとにファイル分割を検討する。

---

---

## 🆕 新規発見：追加対応が必要な問題（2026-03-31 厳格レビュー）

### 🔴 P0 - 本番使用前に必須対応（セキュリティ重大）

#### CSRFトークンが実装されていない（`server.js` 全体）

POST/DELETE/PUTリクエストにCSRF対策がない。`x-session-token` はセッション認証用であり、CSRF対策ではない。

- **影響**: ユーザーがログイン中に、外部の悪意あるサイトから不正なAPIリクエストが実行可能
- **対策**: CSRFトークンの実装、またはSameSiteクッキー属性の明示設定

#### 暗号化キーにフォールバック値がある（`server.js:12`）

```javascript
const ENC_KEY = process.env.PASSWORD_ENC_KEY
  ? Buffer.from(process.env.PASSWORD_ENC_KEY, 'hex')
  : crypto.createHash('sha256').update(process.env.KV_REST_API_TOKEN || 'internal-fallback-key').digest();
```

環境変数未設定時に `'internal-fallback-key'` という固定値で暗号化される。

- **影響**: キーが事実上公開されており、全環境のパスワードが復号可能
- **対策**: フォールバック値を削除し、`PASSWORD_ENC_KEY` 未設定時はサーバー起動を失敗させる

#### GeminiのAPIキーがURLに直接含まれる（`index.html:842`）

```javascript
fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}`, ...)
```

- **影響**: ブラウザ履歴・プロキシログ・ネットワーク監視でAPIキーが露出。第三者による無制限の利用が可能
- **対策**: バックエンド経由でGemini APIを呼び出す。フロントエンドにAPIキーを渡さない

#### Zoho Client Secretがフロントエンドに保存される（`index.html:1359`）

```javascript
<input type="password" value={cfg.clientSecret} onChange={...} />
```

Client Secretが`window.__appData`に保持され、デベロッパーコンソールから読み取り可能。

- **影響**: XSSが発生すると、または悪意あるユーザーが Zoho API を不正利用できる
- **対策**: Client Secretはサーバー側の環境変数にのみ保存。フロントエンドには一切送信しない

#### セッショントークンがlocalStorageに保存（`index.html:4666`）

```javascript
localStorage.setItem('session_token', token);
```

- **影響**: XSS攻撃でトークンが盗聴され、セッションハイジャックが可能
- **対策**: `Secure`・`HttpOnly`・`SameSite=Strict` 属性付きクッキーを使用

#### Zoho webhookに認証がない（`server.js:744`）

```javascript
app.post('/api/zoho/webhook', async (req, res) => {
  const { module, ids } = req.body;
  // requireAuth なし
```

- **影響**: 誰でもこのエンドポイントを叩き、リードデータを改ざん可能
- **対策**: Zohoが提供するwebhook署名検証を実装

#### postMessageにoriginチェックがない（`server.js:507`）

```javascript
window.opener.postMessage("zoho_auth_success", "*")
```

- **影響**: 任意のオリジンが認証完了メッセージを受信可能。認証状態が悪意あるサイトに傍受される
- **対策**: `"*"` を `window.location.origin` に変更

#### Open Redirect の危険性（`server.js:473` + `index.html:1359`）

`redirectUri` がユーザー入力で設定可能（設定画面の `<input type="text">`）。encodeURIComponent はされているが、URLとしての正当性チェックがない。

- **影響**: `redirectUri` を攻撃者のドメインに設定すると、Zoho認証コードが漏洩
- **対策**: `redirectUri` をサーバー側で固定値に制限。設定画面からの変更不可にする

---

### 🔴 P1 - 高優先度（バグ・機能不全）

#### レート制限がKV障害時に無効化される（`server.js:44-51`）

```javascript
try {
  const count = await kv.incr(key);
  ...
} catch { /* KVエラー時はスルー */ }
next();  // 障害時もリクエストが通過する
```

- **影響**: 外部サービス障害時にレート制限が完全に無効化され、ブルートフォース攻撃が通る
- **対策**: KV障害時は安全側に倒す（429を返す、またはリクエストをブロック）

#### Zoho webhookの同時処理でデータロスが発生（`server.js:799`）

複数のwebhookが同時に到着した場合、それぞれが全データを読み込んで上書きするため、後から書いた方が先の変更を消す（Last-Write-Wins問題）。

- **影響**: 複数のZohoイベントが同時処理されるとリードデータが失われる
- **対策**: 更新処理にロック機構またはアトミック操作を導入

#### Zohoトークンリフレッシュの競合状態（`server.js:410`）

複数のリクエストが同時にトークン期限切れを検出し、全てが`refreshZohoToken()`を呼び出す。

- **影響**: 短時間に複数回のトークンリフレッシュが発生、古いトークンを使い続ける処理が混在する可能性
- **対策**: リフレッシュ中フラグを設け、進行中は待機させる

#### API失敗時のロールバックがない（`index.html:64-69`）

```javascript
window.__appData.leads = leads;  // メモリ上はすぐ更新
apiPost('/api/leads', leads);    // 戻り値確認なし・失敗時の復元なし
```

- **影響**: ネットワークエラーなどでAPIが失敗しても、画面上は保存済みに見える。再読み込みすると変更が消える
- **対策**: `apiPost` の結果を確認し、失敗時はメモリ上のデータを元に戻す

#### 招待コードがr.okチェックなしで処理される（`index.html:4820`）

```javascript
const r = await fetch('/api/invite', {...});
const data = await r.json();  // 4xx/5xxでも実行される
setInviteCode(data.code);
```

- **影響**: エラーレスポンス時も `r.json()` を呼び出し、`undefined` のコードがセットされる
- **対策**: `if (!r.ok) throw new Error(...)` を追加

---

### 🟠 P2 - 中優先度

#### セッション有効期限が30日と長すぎる（`server.js:122`）

```javascript
await kv.set('session:' + token, id, { ex: 60 * 60 * 24 * 30 });
```

- **影響**: デバイス紛失・盗難時に30日間アクセスが可能な状態が続く
- **対策**: 7日以下に短縮。または定期的なセッション更新を実装

#### ページネーション未実装（`index.html:35`）

全リードデータを `window.__appData` にまるごと保持。リード数が増えると全件メモリ展開される。

- **影響**: リード数が1000件を超えると、読み込み・保存・描画すべてで遅延・フリーズが発生
- **対策**: サーバー側ページネーション、またはバーチャルスクロールの実装

#### パスワードをハッシュと暗号化の両方で保存（`server.js:156-159`）

```javascript
password: hashedPassword,     // bcrypt（復号不可）
password_enc: encryptedPassword,  // AES-256（復号可能）
```

ログイン認証にはbcryptで十分なはずで、`password_enc`（復号可能な暗号化）を保存する設計意図が不明。

- **影響**: `password_enc` が漏洩した場合、元のパスワードが復元される
- **対策**: `password_enc` の必要性を再検討。管理者がパスワードを確認する要件がないならば削除

#### Google祝日APIへの外部依存（`index.html:219`）

```javascript
const res = await fetch('https://holidays-jp.github.io/api/v1/date.json');
```

起動時に外部APIを呼び出している。失敗すると祝日判定が空のまま動作する。

- **影響**: ネットワーク制限環境・外部サービス障害時に祝日が全て無視される
- **対策**: エラー時はローカルのJP_HOLIDAYSにフォールバックするよう修正

#### エラーメッセージに技術用語が含まれる（`index.html:873`）

「ブラウザのCORSエラーが原因の場合はローカルサーバー経由（http://localhost）でアクセスしてください」

- **影響**: 一般ユーザーにはCORSの意味が不明で、対処できない
- **対策**: 「接続できませんでした。管理者にお問い合わせください」などに変更

#### OAuthエラーハンドリングのコード重複（`index.html:277` と `index.html:955`）

`redirect_uri_mismatch` などのOAuthエラー判定が2か所で全く同じ内容で繰り返されている。

- **対策**: 共通関数に切り出す

#### `ref.current` を `useEffect` の依存配列に含む（`index.html:402`）

```javascript
React.useEffect(()=>{
  ...
  return () => { document.removeEventListener("mousedown", h); };
}, [ref.current]);  // ref.current はオブジェクトの参照先が変わることがある
```

- **影響**: 特定の条件でイベントリスナーが二重登録され、クリックイベントが二重発火する可能性
- **対策**: 依存配列を `[]` にし、`ref` オブジェクト自体を渡す

---

### 優先度まとめ（新規発見分）

| 優先度 | 問題 | 箇所 |
|--------|------|------|
| 🔴 P0 | CSRFトークン未実装 | `server.js` 全体 |
| 🔴 P0 | 暗号化キーのフォールバックが脆弱 | `server.js:12` |
| 🔴 P0 | GeminiのAPIキーがURLに露出 | `index.html:842` |
| 🔴 P0 | Zoho Client Secretがフロントエンドに保存 | `index.html:1359` |
| 🔴 P0 | セッショントークンがlocalStorageに保存 | `index.html:4666` |
| 🔴 P0 | Zoho webhook に認証がない | `server.js:744` |
| 🔴 P0 | postMessage の origin チェックなし | `server.js:507` |
| 🔴 P0 | Open Redirect（redirectUri） | `server.js:473` |
| 🔴 P1 | レート制限がKV障害時に無効化 | `server.js:44` |
| 🔴 P1 | webhook同時処理でデータロス | `server.js:799` |
| 🔴 P1 | Zohoトークンリフレッシュの競合状態 | `server.js:410` |
| 🔴 P1 | API失敗時のロールバックなし | `index.html:64` |
| 🔴 P1 | 招待コードのr.okチェックなし | `index.html:4820` |
| 🟠 P2 | セッション有効期限が30日 | `server.js:122` |
| 🟠 P2 | ページネーション未実装 | `index.html:35` |
| 🟠 P2 | パスワードの二重保存設計 | `server.js:156` |
| 🟠 P2 | 外部祝日APIのフォールバックなし | `index.html:219` |
| 🟠 P2 | エラーメッセージに技術用語 | `index.html:873` |
| 🟠 P2 | OAuthエラーハンドリングの重複 | `index.html:277, 955` |
| 🟠 P2 | `ref.current` の依存配列問題 | `index.html:402` |

---

## 良い点（変わらず）

- KPIカードの視認性が高く、数値が一目でわかる
- LeadCombobox のインクリメンタルサーチ（入力しながら絞り込む）が使いやすい
- モバイル対応がされていてボトムナビも直感的
- エラーバウンダリが実装されており、予期しないエラーに強い
- AI解析結果（興味度・トークポイント・ネクストアクション日）が実用的
- 候補日検索の曜日・バッファ・対象外時間帯の設定が柔軟
- Zoho CRM との双方向連携により、営業フローの自動化が大幅に進んだ
